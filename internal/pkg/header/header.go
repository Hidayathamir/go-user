// Package header contains http header related.
package header

// http header key.
const (
	ContentType   = "Content-Type"
	Authorization = "Authorization"
)

// http header value.
const (
	AppJSON = "application/json"
)
